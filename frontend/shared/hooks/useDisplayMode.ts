// Re-export useDisplayMode from the context
export { useDisplayMode, DisplayModeProvider } from '../contexts/DisplayModeContext';
export type { DisplayMode } from '../contexts/DisplayModeContext';

